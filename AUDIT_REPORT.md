# Отчёт аудита релиз-кандидата — лендинг «Анна Волкова»

Дата аудита: 2026-09-22  
Объект: `D:\123Free\les2\Z5` (PHP-форма `send.php`, статика `site/`, автотесты `tests/run_tests.php` — 26/26 PASS)  
Режим: read-only; изменён только этот файл.

## Сводная таблица находок

| # | Проблема | Риск | Доказательство до исправления | План исправления | Проверка после исправления | Статус |
|---|----------|------|-------------------------------|------------------|----------------------------|--------|
| 1.1 | Файл `logs/submissions.log` с ПДн (имя, телефон, ник, IP) создаётся без явных прав `0600` — на shared hosting может читаться другими системными пользователями | средний | `send.php:170–179, 215–219`: `file_put_contents($logFile, …)` без `chmod`; после заявки `ls -l logs/submissions.log` → права по umask (часто `0644`) | После создания файла лога и `rate-limit.json` выставлять `chmod … 0600` (или `fopen` + `fchmod`) | `ls -l logs/submissions.log` → `-rw-------`; чтение другим пользователем → отказ | не исправлено |
| 1.2 | Защита `config/` и `logs/` от прямого HTTP-доступа — только Apache `.htaccess`; на nginx / без `AllowOverride` файлы отдаются | средний | В проекте только `config/.htaccess`, `logs/.htaccess`; на хостинге без поддержки `.htaccess` `curl https://site/logs/submissions.log` вернёт содержимое (200) | Добавить в README/деплой обязательные блоки nginx `location ^~ /logs/ { deny all; }` и `location ^~ /config/ { deny all; }`; чек-лист «403» | `curl -sI …/logs/submissions.log` и `…/config/managers.local.php` → `403` (или `404`) | не исправлено |
| 2.2 | Rate-limit привязан только к `REMOTE_ADDR`: за общим reverse-proxy все клиенты делят один лимит 5/60с (DoS законных посетителей); слепое доверие к `X-Forwarded-For` дало бы обход | средний | `send.php:65, 87–97` — ключ лимита `$_SERVER['REMOTE_ADDR']`; нет списка доверенных прокси | Если прокси свой — брать IP из `X-Forwarded-For` только при `REMOTE_ADDR` ∈ доверенные прокси; иначе оставить `REMOTE_ADDR` и документировать ограничение | Два клиента за прокси: у каждого до 5×200; подделанный `X-Forwarded-For` снаружи → лимит не обходится | не исправлено |
| 2.3 | Нет CSRF-токена; сервер принимает form-urlencoded через `$_POST`-фолбэк — спам-заявки с чужих сайтов через обычный `<form>` | низкий | `send.php:48–51` (`$data = $_POST`); POST-тест: форма с другого origin с `name/phone/method=call/consent=1` проходит валидацию (CORS form-постам не мешает) | Принимать только JSON (проверять `Content-Type`, не откатываться на `$_POST`) либо добавить CSRF-token в форму | Form-urlencoded POST → `400`/`415`; обычная JSON-отправка с сайта → работает | не исправлено |
| 3.1 | ПДн в `logs/submissions.log` без ротации/очистки — нарушает практику сроков хранения 152-ФЗ (политика есть, механизма нет) | средний | `send.php:171–179` — JSON с `name`, `phone`, `username`, `ip`; старые записи копятся бессрочно, регламента очистки в README нет | Скрипт/cron: удалять записи старше N дней (напр. 90) или ротировать; зафиксировать регламент в README | Запись с подделанным `ts` старше N → после очистки отсутствует | не исправлено |
| 3.2 | `rate-limit.json`: ключи «мёртвых» IP не удаляются (фильтруются только таймстемпы текущего запроса) — файл растёт | низкий | `send.php:84–97` — `$hits` фильтруется только для текущего IP; после активности многих IP в файле остаются IP с пустыми массивами | При каждой записи удалять IP, у которых после фильтрации пустой список хитов | После окна >60 сек и нового запроса — нет ключей с пустыми старыми массивами | не исправлено |
| 3.3 | В режиме `test` ответ `ok:true` не зависит от факта записи в лог (`$saved` не влияет на HTTP-ответ) — потеря заявок при недоступной `logs/` | низкий | `send.php:168–188`: `$saved` вычисляется, но не используется в ответе; chmod-запрет на запись в `logs/` → клиент видит успех, в логе пусто | Если `!$saved` → `500` с русским сообщением и телефоном (как в ветке `real`) | Запрет записи → `500`; возврат прав → `200` и строка в логе | не исправлено |
| 4.1 | Нет ограничения размера тела запроса перед чтением `php://input` — DoS памятью/CPU | средний | `send.php:46–47` — `file_get_contents('php://input')` без лимита; `curl --data-binary @bigfile` (сотни МБ) читается целиком до rate-limit | До чтения: `CONTENT_LENGTH` > 16–64 КБ → `413`; читать с жёстким лимитом байт | POST 1 МБ → `413`, память PHP не растёт; маленький POST → `200` | не исправлено |
| 4.2 | Поле `name`: только длина, без нормализации управляющих символов/переносов — мусор и spoofing в письме/логе | низкий | `send.php:102–107`, `js/script.js:139–146`: `\r\n` и управляющие коды в имени проходят валидацию и уходят в лог/письмо | `preg_replace('/[\x00-\x1F\x7F]/u', '', $name)` + `trim`, затем прежняя проверка длины | POST с `name` содержащим `\r\nBcc:` → в логе/письме одна строка, без перевода | не исправлено |
| 4.3 | Гонка rate-limit: чтение/проверка/запись без общей `flock` на чтении — бурст параллельных POST может пробить лимит 5/60 | низкий | `send.php:77–97` — `LOCK_EX` только на `file_put_contents`; 10 параллельных POST с одного IP могут увидеть старое состояние | Открыть файл и держать `flock($fh, LOCK_EX)` на весь цикл чтение→проверка→запись | Бурст 10 параллельно → ровно 5×`200`, остальные `429` | не исправлено |
| 5.1 | Отсутствует Content-Security-Policy (и meta-CSP в HTML) — нет глубины обороны на случай будущего XSS | низкий | `send.php:10–11` — только `X-Content-Type-Options: nosniff`; в HTML CSP нет; текущих XSS-векторов не найдено | CSP: `default-src 'self'; img-src 'self'; style-src 'self' 'unsafe-inline'; script-src 'self'; base-uri 'none'; form-action 'self'` (заголовок или meta) | Инлайн/чужой `<script>` блокируется CSP; сайт, шрифты и картинки работают | не исправлено |
| 5.2 | Нет anti-clickjacking / referrer-заголовков — страницу можно встроить в iframe чужого сайта | низкий | Нет `X-Frame-Options` / `frame-ancestors` / `Referrer-Policy` в ответах и HTML | `X-Frame-Options: SAMEORIGIN` (или CSP `frame-ancestors 'self'`), `Referrer-Policy: strict-origin-when-cross-origin` | Встраивание в чужой iframe блокируется; форма на своём домене работает | не исправлено |
| 6.1 | `body { overflow-x: hidden }` маскирует горизонтальный скролл вместо ремонта причины — контент на узких экранах может обрезаться без видимого скролла | низкий | `css/style.css:70` — `overflow-x: hidden` на `body`; любая ошибка вёрстки вбок не видна как скролл, текст обрезается | Убрать `hidden` после локализации причин вылета; страховать `max-width:100%` у media и `overflow-wrap` (частично уже есть) | На 320/375: `document.documentElement.scrollWidth <= clientWidth`, весь текст виден | не исправлено |
| 6.2 | Honeypot абсолютно позиционирован `left:-9999px` — в отдельных браузерах off-screen absolute расширяет scroll area; связан со страховкой 6.1 | низкий | `css/style.css:594–601` — `.honeypot { left:-9999px … }` + `overflow:hidden` у самого блока; при удалении 6.1 риск горизонтального скролла | Вместо `-9999px` — техника скрытия без влияния на layout (`left:-10000px; width:1px; height:1px` уже частично) или clip-path | Без `overflow-x:hidden` у body на 320px `scrollWidth` не больше `clientWidth` | не исправлено |
| 7.1 | Автотесты формы не подключены к CI — регрессия доезжает до продакшена без красного прогона | средний | `package.json:6–10` — скрипты есть; `.github/workflows/*` отсутствует; сломанный assert в `send.php` при push ничего не уронит | Workflow на push/PR: `node --check`, `php tests/run_tests.php`, `node scripts/build.js` | Осознанный слом assert → CI красный; откат → зелёный | не исправлено |
| 7.2 | Нет JS-тестов на маску телефона, условные поля и `resetForm` — только `node --check` | низкий | `js/script.js:38–50, 77–90, 203–221` — логика не покрыта unit-тестами (регрессия неочистки формы уже случалась) | 5–10 unit-тестов (node:test/vitest) на `maskPhone`, `validate`, `resetForm` с выносом чистых функций | Намеренная поломка маски → падение теста | не исправлено |
| 7.3 | `npm run test:php` требует `php` в PATH — на машине/окружении без PHP тесты молча не запускаются | низкий | `package.json:9`; в сессии PHP был не в PATH (`C:\Tools\php-portable\php.exe`) | В README/CI зафиксировать PHP ≥8.0 и способ установки; CI ставит PHP явно | Чистый CI-образ → зелёный прогон `php tests/run_tests.php` | не исправлено |
| 1.3 | Нет поддержки переменных окружения для секретов — значения только в PHP-файле, уезжают вместе с копией проекта | низкий | `send.php:21–43`, `config/managers.php` — env не читаются; секреты возможны только в `managers.local.php` | Приоритет `getenv('TO_EMAIL') ?: $config['TO_EMAIL']` (и аналогично для остальных ключей) | Задан env → уведомление уходит на env-адрес без правки файла | не исправлено |

## Положительные находки

1. Секретов в дереве нет: только плейсхолдеры (`example.ru`, `MAIL_MODE=test`); `config/managers.local.php` в `.gitignore`; шаблон отделён от локального конфига.
2. Закрыт веб-доступ к `config/` и `logs/` на уровне Apache (`.htaccess`), в README есть чек-лист «403».
3. Honeypot отвечает тихо и **до** rate-limit/валидации — боты не сжирают лимит и не получают деталей ошибок.
4. Полная серверная валидация, дублирующая клиент: согласие обязательно (400 без него), allowlist способов связи, строгий ник `[A-Za-z0-9_]{4,}`, телефон 10–15 цифр, длина имени ≤120.
5. Пользовательский ввод не возвращается в JSON-ответ — reflected XSS через сообщения об ошибках закрыт структурно.
6. Во всём клиентском коде вывод только через `textContent` / `createTextNode`; `innerHTML` / `insertAdjacentHTML` / `document.write` / `eval` отсутствуют (grep — 0 совпадений).
7. Rate-limit 5/60 с JSON-хранилищем есть и покрыт тестами (6-я заявка → 429).
8. Политика 152-ФЗ: `noindex`, чекбокс без pre-checked, ссылка в новой вкладке, текст про РФ без трансграничной передачи.
9. Ноль внешних CDN/аналитик: локальные woff2, `package.json` без `dependencies` — минимальная supply-chain атака.
10. Сборка статики изолирована: в `site/` не уезжают `send.php`, `config/`, `logs/`, `tests` (страховка в `scripts/build.js:64–71` + `.gitignore`).
11. Мобильная база по ТЗ: база 16px, поля 16px (iOS без авто-зума), кнопки 54px, липкая CTA с `env(safe-area-inset-bottom)`, `prefers-reduced-motion`, без `user-scalable=no`.
12. Автотесты формы реальные (через `php -S`), 26/26 PASS, включая honeypot, согласие и rate-limit; `node --check` и `php -l` чистые.
13. Демо-режим GitHub Pages изолирован на клиенте и явно помечен в UI (бейдж + ссылка на `zayavka.html`) — заявки «в никуда» со статики не уходят.

## Известные ограничения

- E-mail в форме не собирается — осознанное решение по ТЗ (контакт — телефон/месенджеры); серверная валидация e-mail не нужна.
- Телефон-заглушка `+7 (900) 000-00-00` в 4 местах `index.html` и плейсхолдеры реквизитов в `policy.html` — ждут данных заказчика (вне кода исправлений).
- SMTP/`MAIL_MODE=real`, реальные почты `TO_EMAIL`/`FROM_EMAIL` — настраиваются заказчиком; в `test` письма не уходят.
- `opencode.json` в проекте не найден — не проверялся и не изменялся.
- Ручная браузерная проверка 320/375 (скриншоты/эмулятор) не автоматизирована — нет e2e/скриншотных тестов.
- Honeypot и rate-limit — эвристики; продвинутый бот с ротацией IP обходит лимит (CSRF/спам частично покрыты только honeypot и 2.3).
- `.htaccess` не проверялся на живом nginx/Apache деплоя — только статически в репозитории.
- Нет git-репозитория в рабочей папке (`Is directory a git repo: no`) — `.gitignore` проверен содержимо, но `git check-ignore` прогнать нельзя.

## Финальное решение о готовности релиза

**Решение: условно готов к релизу после закрытия средних находок; критических и высоких рисков не найдено.**

- К критическим/высоким — **нет**. Блокеров «нельзя публиковать» в коде не выявлено: XSS-вывод закрыт, секретов нет, согласие и валидация серверные, тесты зелёные.
- **Обязательно до публикации на реальный домен (средние):** 1.1 (права на логи), 1.2 (deny `logs/`+`config/` на целевом веб-сервере + проверка 403), 4.1 (лимит POST-тела), 3.1 (регламент очистки ПДн-лога), 2.2 (корректный rate-limit за прокси, если прокси есть), 7.1 (хотя бы минимальный CI).
- **После релиза / бэклог (низкие):** 2.3, 3.2, 3.3, 4.2, 4.3, 5.1, 5.2, 6.1, 6.2, 7.2, 7.3, 1.3.
- **Демо на GitHub Pages / статика `site/`:** допустимо сейчас — PHP-часть не публикуется, риски 1.x/3.x/4.x на статике неприменимы; ограничения деплоя — раздел «Известные ограничения».
- После правки средних пунктов: прогнать `php tests/run_tests.php` (ожидание 26/26 PASS), `node --check js/*.js`, `node scripts/build.js`, ручную проверку формы и `curl -I` на `logs/` и `config/`.

**Статус релиз-кандидата:** не исправлено (средние риски открыты) — к публикации на боевой хостинг с PHP **только после** закрытия 1.1, 1.2, 3.1, 4.1, 2.2 и 7.1; на статический хостинг — можно сейчас.
