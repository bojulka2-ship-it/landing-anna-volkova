<?php
/**
 * Шаблон конфигурации менеджера.
 * В git — только плейсхолдеры. Реальные значения — в managers.local.php (в .gitignore).
 */

return [
    // Режим письма: 'test' — пишет в logs/submissions.log, письмо не шлёт;
    //               'real' — отправляет письмо через PHP mail().
    'MAIL_MODE' => 'test',

    // Почта менеджера (куда приходят заявки).
    'TO_EMAIL' => 'manager@example.ru',

    // Адрес отправителя (должен быть на домене сайта для SPF/DKIM).
    'FROM_EMAIL' => 'no-reply@example.ru',

    // Тема письма.
    'SUBJECT' => 'Новая заявка с лендинга — репетитор английского',

    // Название сайта (для писем и логов).
    'SITENAME' => 'Лендинг — репетитор английского',
];
